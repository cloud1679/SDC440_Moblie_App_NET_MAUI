<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:Local_Data_Storage_and_Access.Models"
             x:Class="Local_Data_Storage_and_Access.MainPage">

    <VerticalStackLayout
        Padding="30,0"
        Spacing="25">

        <Label
            Text="Richard Burns - Local Storage"
            Style="{StaticResource Headline}"
            SemanticProperties.HeadingLevel="Level1"/>

        <Grid
            RowDefinitions="Auto,Auto,Auto"
            ColumnDefinitions="Auto,*">

            <Label
                Grid.Row="0"
                Grid.Column="0"
                Text="First Name:"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level2"
                VerticalTextAlignment="Center"/>

            <Entry
                Grid.Row="0"
                Grid.Column="1"
                x:Name="txtFirstName"
                Placeholder="Enter your first name"/>

            <Label
                Grid.Row="1"
                Grid.Column="0"
                Text="Last Name:"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level2"
                VerticalTextAlignment="Center"/>

            <Entry
                Grid.Row="1"
                Grid.Column="1"
                x:Name="txtLastName"
                Placeholder="Enter your last name"/>

            <Label
                Grid.Row="2"
                Grid.Column="0"
                Text="Date of Birth:"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level2"
                VerticalTextAlignment="Center"/>

            <DatePicker
                Grid.Row="2"
                Grid.Column="1"
                x:Name="dpDateOfBirth"
                Format="MM-dd-yyyy"/>

        </Grid>

        <Button
            x:Name="btnSave"
            Text="Save Person"
            HorizontalOptions="Center"
            Clicked="OnSaveClicked"/>

        <CollectionView
            VerticalOptions="Fill"
            ItemsSource="{Binding People}"
            SelectionMode="Single">

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Person">

                    <Grid
                        ColumnDefinitions="*,*,Auto"
                        Padding="20">

                        <Label
                            Grid.Column="0"
                            Text="{Binding FirstName}"
                            VerticalTextAlignment="Center"/>

                        <Label
                            Grid.Column="1"
                            Text="{Binding LastName}"
                            VerticalTextAlignment="Center"/>

                        <Label
                            Grid.Column="2"
                            Text="{Binding DoB, StringFormat='{0:MM-dd-yyyy}'}"
                            VerticalTextAlignment="Center"/>

                    </Grid>

                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>

    </VerticalStackLayout>

</ContentPage>